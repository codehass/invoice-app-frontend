import styled from "styled-components";

const StyledContainer = styled.div`
	background-color: var(--color-gray-100);
	color: red;
`;

function SideBar() {
	return <StyledContainer>SideBar</StyledContainer>;
}

export default SideBar;
